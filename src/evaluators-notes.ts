export function createEvaluatorsSummary() {
  return { scope: "evaluators", status: "ready" };
}

// current lane: evaluators
export function evaluatorsTask() {
  return { scope: "evaluators", status: "ready" };
}

// forced-evaluators-2
